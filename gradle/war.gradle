apply plugin: "war"

bootWar {
    mainClassName = "com.jhtest.library.LibraryApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
